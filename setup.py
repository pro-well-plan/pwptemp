from distutils.core import setup
setup(
  name = 'pwptemp',
  packages = ['pwptemp'], # this must be the same as the name above
  version = '0.1',
  description = 'my description',
  author = 'jcamiloangarita',
  author_email = '',
  url = 'https://github.com/pro-well-plan/pwptemp', # use the URL to the github repo
  keywords = ['testing', 'logging', 'example'],
  classifiers = [],
)